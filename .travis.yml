dist: xenial
language: python
python:
  - "3.8-dev"
  - "3.7"
  - "3.6"

# install dependencies
install:
  - pip install -r requirements.txt
  - pip install --user codecov

# run tests
script:
  - pytest

# run codecov
after_success:
  - codecov
